version: 1

project:
  name: com.psu.su_bill_manager
  version_command: echo 1.0

build:
  cmake:
    source_dir: .
    extra_variables:
      - TEST=ON
    #cpack:

appimage:
  linuxdeploy:
    plugins:
      - gtk